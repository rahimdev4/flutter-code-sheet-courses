import 'package:flutter/material.dart';
class AppColors {
  static const background = Color(0xFF050F19);
  static const card = Color(0xFF0B1A2A);
  static const accent = Color(0xFF00D9FF);
  static const success = Color(0xFF00C896);
  static const error = Colors.redAccent;
}
